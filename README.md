# LLM Repro & Trace Kit

A small Typescript + React project that demonstrates: 
- LLM debugging (reproducibility, error capture, rate limit handling)
- Docker + Kubernetes deployment (kind)

> Note: This project keeps OpenAI keys server-side only.
